{% extends 'admin/base.html.twig' %}

{% block title %}Modifier une plateforme{% endblock %}

{% block body %}
    <div class="card shadow mb-4">
		
		<div class="card-header py-3 d-flex justify-content-between">
			<h6 class="m-0 font-weight-bold text-primary">Modifier une plateforme</h6>
			{{ include('admin/platform/_delete_form.html.twig') }}
		</div>
		<div class="card-body">
			{{ include('admin/platform/_form.html.twig', {'button_label': 'Modifier'}) }}
		</div>
	</div>
{% endblock %}
